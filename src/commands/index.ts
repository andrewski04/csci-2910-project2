// export all commands here. this will act as a repository so code can access every command as a list.
import * as cole from "./cole";
import * as blackjack from "./blackjack";

export const commands = {
  cole,
  blackjack,
};
